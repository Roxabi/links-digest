# Vendored from Roxabi/roxabi-plugins `roxabi_sdk/`

Source of truth for shared browser bootstrap. Copied for standalone
container builds (intel-scrape image) where the monorepo path
`roxabi-plugins/` is not present.

When updating: re-copy from roxabi-plugins and keep this note.
